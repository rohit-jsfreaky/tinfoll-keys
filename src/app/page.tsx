/**
 * The root URL is the game. A judge clicks one link and is in the room —
 * nothing to navigate, nothing to read first. `/lab` stays as the Phase 1
 * hit-detection bench and `/room` remains an alias.
 */
export { default } from "./room/page";
